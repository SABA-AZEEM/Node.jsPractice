const os=require("os");
//check arachitecture of os
    // console.log(os.arch());
//check free memory of our os in gb
    // const freeMem=os.freemem();//in bytes
    // console.log(`Free memory:${(freeMem/1024**3).toFixed(2)}`);//convert in GB
//total memory in os
    // const totalMem=os.totalmem();
    // console.log(`Total Memory:${(totalMem/1024**3).toFixed(2)}`);//convert in GB
//host name
    // console.log(os.hostname());
